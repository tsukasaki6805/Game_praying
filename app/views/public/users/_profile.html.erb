<div class="container">
  <div class="row">
    <div class="col-xs-3">
    <h1></h1>
    <%# byebug %>
    <%= attachment_image_tag current_user, :profileimage, :fill, 60, 60, format:'jpeg', class:"img-circle pull-left profile-img", fallback:"no_image.jpg" %>
    <table class="table">
      <tr>
        <th>nickname</th>
        <th><%= current_user.nickname %></th>
      </tr>
      <tr>
        <th>Introduction</th>
        <th><%= current_user.introduction %></th>
      </tr>
      <tr>
       <th>follow</th>
         <th><%= link_to "#{current_user.followings.count} フォロー", followings_public_user_path(user) %></th>
      </tr>
      <tr>
       <th>follower</th>
         <th><%= link_to "#{current_user.followers.count} フォロー", followers_public_user_path(user) %></th>
      </tr>
    </table>
    <%= link_to(edit_public_user_path(user), class:"btn btn-default btn-block edit_user_#{current_user.id}") do %>
    <span class="glyphicon glyphicon-wrench" aria-hidden="true"></span>
    <% end %>
    </div>
  </div>
</div>